#!/usr/bin/tcsh -f
foreach json (*.json)
    `realpath $0 | xargs dirname`/extract_results $json -clean
end
if($#argv > 0) then
    if("-clean" == "$argv[1]") then
        exit(0)
    endif
endif
foreach json (*.json)
    `realpath $0 | xargs dirname`/extract_results $json
end
